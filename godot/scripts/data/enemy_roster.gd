class_name EnemyRoster
extends RefCounted

## 敵の一覧と、どの段でどのレベルが出るか。
## archive/flask-prototype/enemy.py の ENEMY_LIST と get_random_enemy に相当する。
##
## 数値はプロトタイプを出発点にしているだけで、手触りで調整する前提。


## 敵の見た目・当たり判定の大きさ(半径)を一律で縮める倍率。盤面を見やすくする
## ためのバランス調整で、表の設計値はそのまま残し、生成時にこれを掛ける。
## 半径は物理にも効く(硬さ=質量×半径²、寿命=rps÷半径)ので、いじったら
## scripts/playtest.sh で勝率を測り直すこと。
const SIZE_SCALE := 0.75


## 「壁支配の是正」(2026-08-01)で物理を接触寄りに振ったぶんの難易度補償。表の設計値は
## そのまま残し、生成時に掛ける(SIZE_SCALEと同じ方式)。
##
## 接触で勝敗が決まるようにすると、接触こそがプレイヤーの札(EDGE/DRILL/質量/回転)の
## 置き場所なので、同じ物理でもプレイヤーだけが一方的に強くなる(ランボット150で
## クリア率 48.7→68.0%)。敵の回転ゲージを厚くして元の勝率へ戻す。
##
## 回転だけを動かすのは、質量と半径が「被/与比」や硬さの不変条件に効くため。
## 被削りは相手の質量に比例し与削りは相手の硬さ(質量×半径²)に反比例するので、
## 質量を一律に上げると被/与比が質量²で膨らみ、ボスが接触トレードの上限
## (test_enemy_roster.gdのBOSS_TRADE_RATIO_CAP)を割る。rpsはこの比に入らない。
## 感度は高く、1.20まで上げるとクリア率42.0%まで落ちる。いじったら
## playtest/measure_wall_share.gd か scripts/playtest.sh で測り直すこと。
const RPS_SCALE := 1.16


## 段(1..9)に対する敵レベル(1..5)。ゴール(段9)がレベル5のボスになる。
## プロトタイプに「エネミーが強くなる周期が変だったので修正（ボスがレベル5に
## なるようにしたい）」というコミットがあり、この式に落ち着いている。
static func level_for_step(step: int) -> int:
	return clampi((step + 1) / 2, 1, 5)


## その段が「帯(同じレベルが2段続く区間)の2段目」か。段1と、レベルが切り替わる段は偽。
## 現行の level_for_step では偶数段がこれに当たるが、式ではなくレベルの一致で判定する
## ——level_for_step を触ったときに、こちらが黙って嘘にならないようにするため。
## 段9(ボス)は段8がLv4なので必ず偽＝**ボスは据え置き**。
static func is_band_second_step(step: int) -> bool:
	return step > 1 and level_for_step(step) == level_for_step(step - 1)


## 帯の2段目で敵のrpsに掛ける倍率の増分。1段目は1.00(素の表のまま)。
##
## **動機: 帯の2段目は必ず楽になる**。2026-08-08 のボット計測(intercept+greedy、
## 各段の「基準レベル・1体」だけを抜いた素の難度。斥候と乱戦は混ぜていない):
##
##   帯Lv1: 段1 98.5% 残65.6%  →  段2 **100.0%** 残**79.6%**
##   帯Lv2: 段3 98.9% 残62.3%  →  段4   99.4% 残 71.8%
##   帯Lv3: 段5 96.1% 残38.4%  →  段6 **100.0%** 残 47.7%
##   帯Lv4: 段7 71.0% 残25.8%  →  段8   95.6% 残 37.0%
##   (「残」＝勝った戦闘で残ったrpsの中央値＝どれだけ楽に勝ったか)
##
## **4帯とも例外なく、2段目の方が残rpsが 6.5〜13.6pt 多い**。敵のレベルは2段に1度しか
## 上がらないのに、**プレイヤーは毎段1〜3枚の札を積む**からで、帯の1段目で釣り合って
## いた強さが2段目では必ずプレイヤー側へ傾く。勝率だけ見ていると段2・段6の100%しか
## 見えないが(`report.md` の自動アラートも段2しか挙げない)、**余裕(残rps)で見ると
## 4帯すべてに同じ穴が空いている**。
##
## コールドプレイ(2026-08-08, seed=48213)の一次証拠: 段2は**1.42秒・3衝突・残76%**で
## 終わった。全9戦で最も短く、発射の角度も引き量も一度も考えなかった。
##
## **のこぎりを平らにするのではなく、谷の底だけを上げる**。「手強い段のあとに一息」の
## リズムは残す価値があるので、帯の1段目には一切触れない。触るのは2段目だけ。
##
## **rpsだけを動かす理由**は RPS_SCALE の注釈と同じ。被削りは相手の質量に比例し、
## 与削りは相手の硬さ(質量×半径²)に反比例するので、質量や半径を動かすと接触トレード
## (test_enemy_roster.gd の被/与比の床・上限)が段によって変わってしまう。rpsはこの比に
## 入らず、硬さ(=rps×質量×半径²)と寿命(rps÷半径)だけを素直に押し上げる。
##
## 値の決め方(各1200ラン。段6・段8は基準Lv・1体戦の勝率/残rps中央値):
##   0.00: 段6 100.0%/47.7  段8 95.6%/37.0   クリア率 57.7%
##   0.12: 段6  96.2%/ ---  段8 94.6%/ ---   クリア率 57.7%
##   0.25: 段6  94.4%/42.0  段8 87.2%/27.4   クリア率 54.3%  ← 採用
##   0.40: 段6  91.7%/41.8  段8 82.1%/28.8   クリア率 52.7%
## 0.25 を採る。0.12 は段8がほぼ動かず(95.6→94.6)谷が残り、0.40 はクリア率を5pt
## 落として「一息つく段」まで削る。0.25 での帯ごとの余裕の差(2段目−1段目)は
##   Lv2 +9.5pt→+4.1pt / Lv3 +9.3pt→+2.9pt / Lv4 +11.2pt→+0.9pt
## と縮み、死亡段の分布も段7・段9への集中がほぐれた
## (段6 4.8→8.5%、段8 3.5→7.0%、段7 17.0→15.0%、段9 27.2→23.2%)。
## 帯の1段目(段1・3・5・7)は1ミリも動かない。
##
## **Lv1帯(段2)だけはこのつまみが効かない**。段2は +40% にしても勝率100.0%・
## 残rps 79.6→78.8 とほぼ不動だった。原因は与削りの天井
## (SpinnerPhysics.capped_spin_drain = 相手の初期rps × drain_cap_share)で、
## **天井が相手のrpsに比例するため、rpsを上げると天井も同率で上がり、倒すのに要する
## 打撃数が変わらない**。素の削りが天井を大きく超えて常に張り付いている帯——つまり
## プレイヤーが圧倒している帯——では、rpsは難易度つまみとして構造的に死んでいる。
## 段2を動かすには硬さ側の別の軸が要る——docs/evolve/journal.md の次の候補に回した。
## いじったら scripts/playtest.sh で段別勝率・残rps・クリア率を測り直すこと。
const BAND_SECOND_STEP_RPS_RAMP := 0.25


## 段stepの敵rpsに掛ける倍率。帯の1段目とボス段は必ず1.0(素の表と厳密に一致)。
static func step_rps_scale(step: int) -> float:
	return 1.0 + BAND_SECOND_STEP_RPS_RAMP if is_band_second_step(step) else 1.0


## 敵1体を、段stepに出る個体として**rpsだけ**押し上げた複製にする。
## 倍率1.0の段(帯の1段目・ボス段)では複製せずそのまま返す＝従来と厳密に一致。
## 名前とレベルは保つ——マップのLv表示・報酬の実レベル・CLIの名前復元が依存している。
## 乱戦の質量倍率(melee_member)とは軸が違うので、順序を問わず重ねられる。
static func step_member(data: EnemyData, step: int) -> EnemyData:
	if data == null or not is_band_second_step(step):
		return data
	var stats := data.stats.duplicate_stats()
	stats.rps *= step_rps_scale(step)
	return EnemyData.make(data.level, data.display_name, stats)


## 数値はテストプレイ(scripts/playtest.sh)で当てたもの。プロトタイプの表は
## 使っていない。あちらは段9のボスが 質量5.0/半径3.0/rps60 で、パーツを8枚
## 積んだプレイヤーでも勝率0%だった(25,000戦で勝利ゼロ)。
##
## この2つが勝敗をほぼ決めるので、そこを見て決めている:
##  - **寿命 = rps ÷ 半径**。自然減衰が半径に比例するため。殴られなくても
##    ここで力尽きる。プロトタイプの敵は寿命20〜30秒で、プレイヤー(強化後で
##    9秒前後)が待つだけで負けていた。
##  - **硬さ = 質量 × 半径²**。1衝突で削られるRPSがこれに反比例する。
##    プロトタイプのボスは45、プレイヤーは0.4しかなく、接触即死だった。
##
## 質量と半径は見た目の個性(小さくて素早い/大きくて重い)として先に決める。
## 同レベルの2体はほぼ同じ強さで、形だけ違う(rpsは形に合わせて僅かに違えてよい)。
##
## **強さはレベルと共に緩やかに上げる**。強さは1つのつまみではなく、硬さ・質量・
## 初期rpsの複合で決まる:
##  - **硬さ = rps × 質量 × 半径²**。倒すのに要する打撃数はこれにほぼ比例する
##    (1衝突のrps減少が 質量×半径² に反比例するため)。レベルと共に上げる主軸。
##  - **寿命 = rps ÷ 半径**。自然減衰が半径比例。殴られなくてもここで力尽きる。
##    プレイヤー(強化後〜9秒)を大きく超える寿命は「待ち負け」を生む。
##  - **rps** は回転ゲージそのもの。**レベルと共に単調増加**させる(低レベルほど
##    ゲージが大きいと直感と食い違う)。かつては勝率つまみとしてrpsを高レベルほど
##    下げており(Lv1=32→Lv4=10.5)、rpsも寿命もレベルと逆転、実プレイでも高レベル段
##    ほど楽という逆転が起きていた(段別勝率が横ばい〜後半で上昇)。それを解消した。
##
## 三者は掛かり合う(硬さ=rps×質量×半径²)ので、全部を急に上げると硬さが跳ねて
## 難易度に崖ができる。レベル間の上昇が緩やかになるよう複合で当てる。ボスは据え置き
## (rps26)で `187caab` の自滅対策を再燃させない。同レベル2体はほぼ同強度に揃える。
##
## 調整の判定は段別勝率を固定ターゲットにせず(全段の目標勝率を決めているわけではない)、
## 「レベルと共に難易度が緩やかに上がるか・即死化していないか・待ち負けが支配的で
## ないか・ボスが据え置き挙動か」の signal で見る。数値をいじったら scripts/playtest.sh で
## 測り直すこと。
static func all() -> Array[EnemyData]:
	return [
		# 各レベル5種。同レベルは硬さ(=強さ)をほぼ揃え、形(質量・半径)で個性を出す。
		# rpsは硬さ一定になるよう形から逆算しているので同レベル内で少し違う。
		# 小さくて素早い。ほぼ負けない導入。
		_enemy(1, "ENEMY_1_1", 0.8, 0.5, 0.97, 1.0, 15.6),
		_enemy(1, "ENEMY_1_2", 0.7, 0.55, 0.98, 1.0, 16.2),
		_enemy(1, "ENEMY_1_3", 1.0, 0.46, 0.97, 1.0, 15.5),
		_enemy(1, "ENEMY_1_4", 0.6, 0.6, 0.98, 1.0, 15.2),
		_enemy(1, "ENEMY_1_5", 0.85, 0.52, 0.975, 1.0, 14.3),
		_enemy(2, "ENEMY_2_1", 1.2, 0.8, 0.97, 1.0, 19.5),
		_enemy(2, "ENEMY_2_2", 1.1, 0.85, 0.98, 1.0, 20.2),
		_enemy(2, "ENEMY_2_3", 1.5, 0.74, 0.97, 1.0, 18.9),
		_enemy(2, "ENEMY_2_4", 0.95, 0.92, 0.985, 1.0, 19.2),
		_enemy(2, "ENEMY_2_5", 1.3, 0.82, 0.98, 1.0, 17.7),
		# Lv3以降は spin_decay<1 で自然減衰を弱め、寿命の逆転を解消する。かつては全敵
		# spin_decay=1.0 で寿命目安(rps÷(半径×spin_decay))がLv1≈42→Lv5≈19とレベルと共に
		# 急落し、強敵ほど「弱発射で離れて置き、自滅を待つ」だけで勝てる受け身支配の
		# 主因だった(敵の敗因の7〜8割が自然減衰)。Lv3+を一律≈26〜29に均し、決着を
		# 接触(削り・壁)へ寄せる。不変条件は tests/test_enemy_roster.gd の寿命テストが守る。
		# 3_3と3_4は質量を半径へ振り替えて接触トレードを均している(旧: 2.5/1.12・
		# 1.55/1.32)。硬さ・耐久・寿命はほぼ揃えてあったのに、被削りは相手質量だけで
		# 決まるため、同レベル内で被/与比が0.95〜1.64と1.7倍開いており、最重の3_3だけ
		# 別格に強かった(素プレイヤーの戦闘勝率 3_3=1.6% vs 3_4=10.0%、コールドプレイ
		# で3_3に5連敗の一次証拠)。硬さ(質量×半径²)を保って質量→半径に振れば、
		# 与削り・耐久・打撃数は不変のまま被削りだけが均される。
		# 不変条件は tests/test_enemy_roster.gd のトレード格差テストが守る。
		_enemy(3, "ENEMY_3_1", 2.0, 1.2, 0.98, 1.0, 21.4, 0.85),
		_enemy(3, "ENEMY_3_2", 1.8, 1.25, 0.985, 1.0, 22.1, 0.85),
		_enemy(3, "ENEMY_3_3", 2.2, 1.19, 0.98, 1.0, 19.8, 0.85),
		_enemy(3, "ENEMY_3_4", 1.7, 1.26, 0.985, 1.0, 22.9, 0.85),
		_enemy(3, "ENEMY_3_5", 2.1, 1.22, 0.982, 1.0, 19.9, 0.85),
		# Lv4は質量を15%落としている(旧: 3.0/2.8/3.6/2.4/3.1)。段7(初のLv4戦)が
		# 全段最深の谷(勝率40.5%・intercept死の20%が集中)で、コールドプレイでも
		# 接触トレードが構造的に不利(等速衝突の被削り/与削り比がedge上限込みで
		# 2.1〜3.2。Lv3は1.3〜1.6)だった。削りも弾きも相手質量に比例するので、
		# 質量だけ落とせば接触戦をフェアに寄せつつ、寿命(rps÷(半径×spin_decay))は
		# 不変=前サイクルの「待てば自滅」対策を戻さない。ボスは据え置き。
		# 不変条件は tests/test_enemy_roster.gd の接触トレード床テストが守る。
		# 4_3と4_4も同じ理由で質量を半径へ振り替え(旧: 3.05/1.5・2.05/1.78)。Lv4の
		# 被/与比は1.71〜2.57と1.5倍開いており、最重の4_3だけ上限(2.7)近くに突出、
		# コールドプレイで4_3に4連敗(戦法3種で喪失内訳がほぼ同一=工夫の余地なし)が
		# 一次証拠。最重を大きく削るとリムの質量可視化の「読める差」(test_disc_weight)
		# が消えるため、最重は小さく削り、最軽の4_4を引き上げて両側から寄せている。
		_enemy(4, "ENEMY_4_1", 2.55, 1.6, 0.98, 1.0, 26.0, 0.75),
		_enemy(4, "ENEMY_4_2", 2.4, 1.7, 0.985, 1.0, 26.7, 0.75),
		_enemy(4, "ENEMY_4_3", 2.95, 1.53, 0.98, 1.0, 25.6, 0.75),
		_enemy(4, "ENEMY_4_4", 2.2, 1.72, 0.985, 1.0, 27.3, 0.75),
		_enemy(4, "ENEMY_4_5", 2.65, 1.62, 0.982, 1.0, 25.5, 0.75),
		# ボス。大きく重く、寿命も硬さもプレイヤーを上回る。5種あり、ランごとに1体が単体で出る。
		# 発射速度はEnemyDataから外し、自機と共通のレンジ(LaunchSpeed。上限12)から出現ごとに
		# 抽選する。かつてボスは全種8.5固定だった(元は11.0)。速いとボスが壁に突撃し、無敵中に
		# rpsの6割超を壁で失って勝手に死ぬ(=GHOSTで待つだけで倒せた)ため落としていた。共通レンジの
		# 上限を12に抑えることで、その暴発を新しい物理法則を足さず既存の発射軌道の範囲で抑える。
		# 追加種も硬さを揃え形だけ違う。
		# Lv5もLv4と同じ理由で質量を15%落としている(旧: 4.5/5.2/3.9/4.6/4.9)。等速衝突の
		# 被削り/与削り比(edge上限込み)が4.0〜5.3で、攻めを積み切っても接触が常に大損
		# だった(コールドプレイでボス5連敗・全て惜敗、edge0.60でも与1.0/hit vs 被3〜4/hitの
		# 一次証拠が2サイクル連続)。質量だけ落とせば接触トレードが3.3〜4.5に寄り、
		# 寿命(rps÷(半径×spin_decay))は不変=「待てば自滅」対策は戻さない。rps・半径は
		# 据え置きで、同レベル内の耐久(rps×質量×半径²)は≈417に揃えてある。
		# 不変条件は tests/test_enemy_roster.gd の接触トレード床テスト(ボス用の上限)が守る。
		_enemy(5, "ENEMY_5_1", 3.8, 2.4, 0.98, 1.0, 33.8, 0.65),
		_enemy(5, "ENEMY_5_2", 4.4, 2.25, 0.98, 1.0, 33.3, 0.65),
		_enemy(5, "ENEMY_5_3", 3.3, 2.55, 0.98, 1.0, 34.6, 0.65),
		_enemy(5, "ENEMY_5_4", 3.9, 2.4, 0.98, 1.0, 33.0, 0.65),
		_enemy(5, "ENEMY_5_5", 4.15, 2.32, 0.98, 1.0, 33.3, 0.65),
	]


static func of_level(level: int) -> Array[EnemyData]:
	var result: Array[EnemyData] = []
	for enemy in all():
		if enemy.level == level:
			result.append(enemy)
	return result


## その段にふさわしい敵を1体選ぶ。
##
## 帯の2段目のrpsランプ(step_member)は**ここで掛ける**。マップ生成には
## `pick_group_for_step` を通らない経路が2つある(1体部屋保証の引き直し
## `_ensure_single_escape` と、斥候の逃げ道 `_ensure_vanguard_choice`。どちらも
## 「その段の通常単体」を引き直す)ので、入り口ごとに掛けていると必ずどれかが漏れる
## ——実際このテストを足した時点で段2の引き直しノードだけ素の強さで残っていた。
static func pick_for_step(step: int, rng: RandomNumberGenerator = null) -> EnemyData:
	if rng == null:
		rng = RandomNumberGenerator.new()
		rng.randomize()
	var candidates := of_level(level_for_step(step))
	if candidates.is_empty():
		push_error("EnemyRoster: 段%dに出せる敵がいない" % step)
		return null
	return step_member(candidates[rng.randi_range(0, candidates.size() - 1)], step)


## 表示名から敵を引く。CLI(naive_play)がリトライで入れ替えた個体をJSONの
## 名前列から復元するために使う。知らない名前はnull。
static func find_by_name(display_name: String) -> EnemyData:
	for enemy in all():
		if enemy.display_name == display_name:
			return enemy
	return null


## リトライ(コンティニュー)用: グループの各個体を「同レベルの別個体」に入れ替える。
## 頭数・並び・各スロットのレベルは保つので、マップ表示のLv[体数]も報酬の
## 実レベル(MapNode.level())も嘘にならない。同レベルに他の個体がいる限り
## 同じ個体は選ばない(現状は全レベル5種なので必ず入れ替わる)。
##
## 発見の経緯: リトライは出現位置しか変わらず、同じ相手に同じ負け方を繰り返す
## 徒労感が強かった(コールドプレイで段5 ENEMY_3_1と3連戦・段8 同型ペアに4連敗、
## 喪失内訳までほぼ同一)。同レベルは硬さをほぼ揃え形で個性を出す設計なので、
## 個体の入れ替えは難易度を保ったまま接触トレードの手触りだけを変える。
##
## 入れ替えた個体にも乱戦の質量倍率(melee_member)と段のrpsランプ(step_member)を
## 掛け直す。素の表から引くとリトライしただけで元の手強さに戻ってしまう
## (予告に出る硬さも嘘になる)。stepは呼び手が渡す。0(既定)は倍率1.0＝従来どおり。
static func reroll_group(
	group: Array[EnemyData], rng: RandomNumberGenerator = null, step: int = 0
) -> Array[EnemyData]:
	if rng == null:
		rng = RandomNumberGenerator.new()
		rng.randomize()
	var result: Array[EnemyData] = []
	for enemy in group:
		var others: Array[EnemyData] = []
		for candidate in of_level(enemy.level):
			if candidate.display_name != enemy.display_name:
				others.append(candidate)
		if others.is_empty():
			result.append(enemy)
		else:
			result.append(step_member(
				melee_member(others[rng.randi_range(0, others.size() - 1)], group.size()), step
			))
	return result


## 斥候(次レベルの単体エリート)が混ざりうる段か。
##
## 帯の2段目(段2・4・6)に加えて、**段1**。段8はLv5=ボスの先取りになり
## 単独ボスの演出が薄れるので対象外(vanguard_levelの上限4)。
## 帯の1段目のうち段3・5・7は除いたまま——段5・段7は次レベルとの初対面が既に崖
## (ラン中勝率 段5=87.8%/段7=60.9%、死亡率も段7=15.8%と全段で最悪)。
## 段3を外した理由は下の実測に書いた。
##
## **なぜ段1を足すのか**(2026-08-07)
## この2段には**リスクが1ミリも無い**。`build/playtest/report.md`(この変更の前に
## 取得)の「どの段で死ぬか」は段4以降しか行が立たず、**段1〜3の死亡率は0.0%**。
## ラン中勝率も 98.4 / 98.4 / 94.9% で、1200ランで誰一人ここで止まっていない。
##
## 一次証拠(コールドプレイ 2026-08-07, seed=4711): 9戦全勝・残機3を1つも使わず
## クリア。段1〜3は撃った後の残rpsが 51 / 62 / 82% で、**どれも1発撃って眺めるだけ**
## だった。とくに段3は決着7.53秒のうち自分が絡んだ衝突が**1回**で、2体目は
## 削り0.0(自分は一度も触っていない)のまま壁で勝手に果てている。
## マップの分岐も、段1〜3は「頭数が多い＝報酬が多い」部屋を選ぶだけの作業で、
## **選ぶこと自体に賭けが無かった**。
##
## 斥候はこの穴にちょうど嵌まる既存の仕組みで、(a)マップ生成時に確定して
## ノードのLv表示に出る＝**見て避けられる**、(b)倒せば実レベル準拠の報酬
## (rare_weight_for_level)が返る、(c)`MapTree._ensure_vanguard_choice` が
## 「進める先が全部斥候」を潰すので**強制にならない**。敵の強さにも物理にも
## 触らずに、無風だった段へ「乗るか降りるか」を1つ置ける。
##
## **なぜ段3には足さないのか**(同日・実測で却下)
## 帯の1段目の斥候は帯の2段目より**1段ぶん前倒しが深い**。帯の2段目の斥候
## (段2のLv2・段4のLv3)は本来の初対面がそれぞれ段3・段5＝1段先だが、
## 帯の1段目だと段1のLv2が段3の敵、**段3のLv3が段5の敵＝2段先**になる。
## 段1と段3の両方へ広げて `scripts/playtest.sh` を取り直したら、
## 段3の勝率が **94.9% → 78.3%(-16.6pt)**、クリア率が **53.3% → 45.3%(-8.0pt)**、
## ボス到達率も 83.7% → 78.3% まで落ちた。**段3のLv3だけが重すぎる**。
## 一方この試行でも段1は 98.4% → 96.5% で、崖にならずに0%だった死亡率だけが立った。
## そこで段1だけを残した。
##
## 段1のLv2は素のステータス相手で単体勝率96.6%(report「戦闘単体」intercept)。
## 崖ではないが**払う代償は実在する**——確認のコールドプレイ(seed=55)では
## 勝ったものの残rpsが**10%**で、通常のLv1部屋(同じ段で51%残)とは別物の1戦だった。
static func is_vanguard_step(step: int) -> bool:
	return step == 1 or (step % 2 == 0 and level_for_step(step) < 4)


## 斥候のレベル(その段の基準レベル+1)。斥候が出ない段は0。
static func vanguard_level_for_step(step: int) -> int:
	return level_for_step(step) + 1 if is_vanguard_step(step) else 0


## 斥候が出る確率。マップ生成のノード単位で振られる。0.2は「多くのマップで
## どこかの段に1〜2ノード現れるが、進路の大半は通常のまま」の狙い
## (1段5ノード前後 × 段1/2/3/4/6)。段が3つ増えても率を下げないのは、
## 1つの段の中での比率(5ノード中1つ前後)が「進路の大半は通常」を決めているから。
const VANGUARD_CHANCE := 0.2


## 乱戦(複数体)メンバーの**質量**に掛ける倍率の指数。倍率 = 頭数^-MELEE_MASS_EXP
## で、1体なら必ず1.00＝単体戦は従来と厳密に同じ数値になる(2体0.84・3体0.76)。
##
## 動機: 乱戦は「倒した頭数だけ報酬が選べる(レアの重みも頭数倍)」代わりに手強い、
## という**選べるリスク**の設計なのに、期待値(報酬枚数×勝率)で見ると
## どの段でも選ぶ意味が無かった(measure_group_size.gd・中盤ビルド):
##   Lv2: 1.00 / 2.00 / 2.96 —— 勝率が 100 / 100 / 98.8% で**取らない理由が無い**
##   Lv3: 0.91 / 0.48 / 0.27 —— 勝率が 91 / 24 / 9% で**取る理由が無い**
## 「頭数ぶんきつい」が Lv2 では床、Lv3 では崖に張り付いており、選択の帯が無い。
## コールドプレイ(2026-08-03)でも段1〜4の乱戦4部屋を無傷で抜けたあと、段5の
## 2体部屋で6連敗して残機5を全部溶かしている(前サイクルも同じ段で5連敗)。
##
## 質量を選ぶ理由。頭数を増やすと「殴られる回数」が増えるので、**1回あたりの重み**を
## 落として釣り合わせる:
##  - 被削りは相手の質量に**比例**し(spin_drain)、与削りは相手の硬さ(質量×半径²)に
##    **反比例**する。質量だけを下げると接触トレードの両側が同時に緩む。
##  - **寿命(rps÷(半径×spin_decay))は質量を含まない**。かつて撤廃された
##    「rpsの頭数割り」と違い、強敵ほど短命=待てば勝てるという受け身支配を戻さない。
##  - 一撃死も戻らない。与削りには capped_spin_drain(相手ゲージの50%)の天井があり、
##    質量を下げても1衝突では倒せない——頭数割りが下限を張って凌いでいた問題は起きない。
##  - レベルは動かさない。ノードのLv表示・報酬のレア重み(実レベル基準)はそのまま。
##
## 指数の決め方は measure_melee_scale.gd の掃引(Lv3・中盤ビルド・各300戦):
##   exp 0.00 → 91 / 24 / 9%   (現行。罠)
##   exp 0.25 → 91 / 59 / 44%  ← 採用
##   exp 0.35 → 91 / 72 / 63%
##   exp 0.50 → 91 / 84 / 89%  (頭数が増えるほど楽になる=逆転)
## 0.25 は「報酬2倍のために4割の敗北を買う」帯で、期待値は 0.91 / 1.18 / 1.31 と
## リスク側をわずかに上に置く。真横に揃えると誰も乱戦を選ばず、頭数ぶんの報酬で
## 回っているパーツ経済(_ensure_single_escapeの注釈参照)が痩せてしまう。
## いじったら measure_melee_scale.gd と scripts/playtest.sh で測り直すこと。
const MELEE_MASS_EXP := 0.25


## 頭数countの乱戦メンバーの質量倍率。1体は必ず1.0(単体戦は従来と厳密に同じ)。
static func melee_mass_scale(count: int) -> float:
	if count <= 1:
		return 1.0
	return pow(float(count), -MELEE_MASS_EXP)


## 敵1体を、頭数countの乱戦メンバーとして質量だけ落とした**複製**にする。
## 1体なら複製せずそのまま返す。複製するのは、同じ個体が群に2度入りうるためで、
## 素の実体を書き換えると倍率が二重に掛かる(all()は毎回新しい実体を作るが、
## 1回の抽選の中では同じ実体が2度appendされうる)。
## 名前とレベルは保つ——マップのLv表示・報酬の実レベル・CLIの名前復元が依存している。
static func melee_member(data: EnemyData, count: int) -> EnemyData:
	if data == null or count <= 1:
		return data
	var stats := data.stats.duplicate_stats()
	stats.mass *= melee_mass_scale(count)
	return EnemyData.make(data.level, data.display_name, stats)


## レベルlevelの敵をcount体、乱戦の規則(melee_member)と段の規則(step_member)を
## 通して引く。乱戦グループを組む場所をここ1つにするための入り口で、マップ生成の
## 昇格補償(MapTree._promote_compensation)も同じ規則を踏む。
## stepは帯の2段目のrpsランプに使う。0(既定)は「段が分からない」＝倍率1.0。
static func group_of(
	level: int, count: int, rng: RandomNumberGenerator, step: int = 0
) -> Array[EnemyData]:
	var candidates := of_level(level)
	var group: Array[EnemyData] = []
	if candidates.is_empty():
		return group
	for _i in maxi(count, 1):
		group.append(step_member(
			melee_member(candidates[rng.randi_range(0, candidates.size() - 1)], count), step
		))
	return group


## その段の出現グループ(1〜3体)を選ぶ。乱戦パターンの入り口。
##
## ほとんどは1体。たまに2〜3体の乱戦になる。複数体でも**単体と同じ、その段の
## レベル**から選ぶ(レベルは下げない。下げると表示レベルが変わり、報酬の実レベルも
## 動く)。手強さの調整は質量倍率(MELEE_MASS_EXP)だけで行う。
## ボス(レベル5)は演出上つねに単体。
##
## 斥候段(is_vanguard_step)では、まれに(VANGUARD_CHANCE)次レベルの敵が**単体で**
## 現れる。マップ生成時に確定するのでノードのLv表示に出る=見て避けられる
## 「選べるリスク」で、倒せば実レベル準拠の報酬(rare_weight_for_level)が返る。
## 単体限定なのは、乱戦×レベル先取りの複合は脅威が読めなくなるため。
## allow_vanguard=false で斥候を封じる(マップ側の「全進路が斥候」の救済用)。
##
## かつては複数体を1段下のレベルから選び、さらに総回転量を一定に保つため各体の
## rpsを頭数で割っていた。だが割ると「衝突1回で終わる」コマになり一撃死が多発
## (3体Lv1で12.7%)、下限を張って凌ぐ始末だった。**その2つは今も採らない**
## (rpsは寿命に効いて受け身支配を戻し、1段下げは表示レベルを変える)。
## 代わりに質量だけを頭数で緩める——理由と実測は MELEE_MASS_EXP の注釈に書いた。
static func pick_group_for_step(
	step: int, rng: RandomNumberGenerator = null, allow_vanguard: bool = true
) -> Array[EnemyData]:
	if rng == null:
		rng = RandomNumberGenerator.new()
		rng.randomize()

	# ボスは単体固定。
	if level_for_step(step) >= 5:
		return [pick_for_step(step, rng)]

	# 斥候: 次レベルの単体。頭数ロールより先に振る(斥候は乱戦にならない)。
	# 斥候にも段のランプを掛ける。掛けないと帯の2段目で「斥候の方が通常戦より
	# 楽な瞬間」ができ、選べるリスクの向きが裏返る。
	if allow_vanguard and is_vanguard_step(step) and rng.randf() < VANGUARD_CHANCE:
		var scouts := of_level(vanguard_level_for_step(step))
		if not scouts.is_empty():
			return [step_member(scouts[rng.randi_range(0, scouts.size() - 1)], step)]

	# 頭数を重み付きで決める。大半は1体、たまに2〜3体。
	var roll := rng.randf()
	var count := 1
	if roll > 0.9:
		count = 3
	elif roll > 0.6:
		count = 2

	if count == 1:
		return [pick_for_step(step, rng)]

	var group := group_of(level_for_step(step), count, rng, step)
	if group.is_empty():
		return [pick_for_step(step, rng)]
	return group


static func _enemy(
	level: int, name_: String,
	mass: float, radius: float, friction: float, restitution: float, rps: float,
	spin_decay: float = 1.0
) -> EnemyData:
	var stats := SpinnerStats.new()
	stats.mass = mass
	stats.radius = radius * SIZE_SCALE
	stats.friction = friction
	stats.restitution = restitution
	stats.rps = rps * RPS_SCALE
	# 既定1.0(Lv1〜2)。Lv3以降は<1にして自然減衰を弱め、寿命の逆転(高レベルほど
	# 短命=待てば勝てる)を防ぐ。上の表の末尾引数がそれ。
	stats.spin_decay = spin_decay
	return EnemyData.make(level, name_, stats)
